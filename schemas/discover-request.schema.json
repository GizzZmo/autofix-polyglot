{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/GizzZmo/autofix-polyglot/schemas/discover-request.schema.json",
  "title": "DiscoverRequest",
  "description": "Body for POST /v1/discover on the Go healer.",
  "type": "object",
  "properties": {
    "urls": {
      "type": "array",
      "items": { "type": "string", "format": "uri" },
      "description": "Batch of absolute URLs to enqueue for verification / healing."
    },
    "url": {
      "type": "string",
      "format": "uri",
      "description": "Optional single-URL convenience field (accepted by current healer)."
    }
  },
  "anyOf": [
    { "required": ["urls"] },
    { "required": ["url"] }
  ],
  "additionalProperties": false
}
